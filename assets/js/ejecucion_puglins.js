$(document).ready(function() {

    /* -------------------------------------------------------------------------- */
    /*                                  LABELAUTY                                 */
    /* -------------------------------------------------------------------------- */
    $(".radio-buttons").labelauty({
        // Use icon?
        // If false, then only a text label represents the input
        icon: false
    });
});